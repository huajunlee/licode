# CLAUDE.md

## Project Instructions
Use the spec files in `docs/specs/` as the source of truth for planned work.

## Workflow
- Keep implementation aligned with `spec.md`.
- Track execution in `tasks.md`.
- Use `checklist.md` before delivery.
